<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#CC1A1A2E"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="32dp">

    <!-- Lock icon -->
    <ImageView
        android:id="@+id/ivLockIcon"
        android:layout_width="96dp"
        android:layout_height="96dp"
        android:src="@android:drawable/ic_lock_lock"
        android:layout_marginBottom="16dp"
        app:tint="#FFFFFF" />

    <!-- App title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="EMI LOCK"
        android:textColor="#FFFFFF"
        android:textSize="28sp"
        android:textStyle="bold"
        android:letterSpacing="0.1"
        android:layout_marginBottom="8dp" />

    <!-- Lock message -->
    <TextView
        android:id="@+id/tvLockMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Your EMI is overdue. Please contact your dealer."
        android:textColor="#FFCCCC"
        android:textSize="15sp"
        android:gravity="center"
        android:layout_marginBottom="32dp" />

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#44FFFFFF"
        android:layout_marginBottom="24dp" />

    <!-- Dealer info card -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#22FFFFFF"
        android:padding="16dp"
        android:layout_marginBottom="24dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="DEALER"
            android:textColor="#AAAAAA"
            android:textSize="11sp"
            android:textAllCaps="true" />

        <TextView
            android:id="@+id/tvDealerName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="—"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            android:layout_marginBottom="8dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="RETAILER"
            android:textColor="#AAAAAA"
            android:textSize="11sp"
            android:textAllCaps="true" />

        <TextView
            android:id="@+id/tvRetailerName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="—"
            android:textColor="#FFFFFF"
            android:textSize="15sp"
            android:layout_marginBottom="4dp" />

        <TextView
            android:id="@+id/tvRetailerPhone"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textColor="#7EB2FF"
            android:textSize="15sp" />
    </LinearLayout>

    <!-- IMEI -->
    <TextView
        android:id="@+id/tvImei"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:textColor="#888888"
        android:textSize="12sp"
        android:layout_marginBottom="24dp" />

    <!-- Unlock code input -->
    <EditText
        android:id="@+id/etUnlockCode"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter 6-digit unlock code"
        android:inputType="numberPassword"
        android:maxLength="6"
        android:textColor="#FFFFFF"
        android:textColorHint="#888888"
        android:backgroundTint="#FFFFFF"
        android:gravity="center"
        android:textSize="20sp"
        android:letterSpacing="0.3"
        android:layout_marginBottom="16dp" />

    <!-- Unlock button -->
    <Button
        android:id="@+id/btnUnlock"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="UNLOCK DEVICE"
        android:textColor="#1A1A2E"
        android:backgroundTint="#7EB2FF" />

</LinearLayout>